// Fibonacci — recursive definition: F(n) = F(n-1) + F(n-2) (https://en.wikipedia.org/wiki/Fibonacci_sequence)
fun fib(n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);
}

for (var i = 0; i <= 12; i += 1)
    printf("fib({}) = {}\n", i, fib(i));
