// Shows how an object instance can be serialized
class Foo {
    init(x, y) {
        this.x = x;
        this.y = y;
    }
}

class Bar {
    init(z, foo) {
        this.z = z;
        this.foo = foo;
    }
}

printf("{}\n", Bar(12.5, Foo(25, "Test")));