# Benchmark Runner

**CMake target:** `CelloxBenchmarks`

This directory builds the benchmark runner executable for Cellox. It executes a
set of predefined benchmark programs and records timing results so runtime and
language changes can be compared over time.

At a high level, this tool exists to measure compiler/runtime performance on a
repeatable suite of representative programs.
